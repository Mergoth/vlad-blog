# CASCADE_HINT: Minimal Netlify config for static Astro + Edge redirect
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # CASCADE: Public GA4 measurement ID passed at build time
  PUBLIC_GA_ID = "G-57N05H2588"

# Edge redirect from root "/" to best locale
[[edge_functions]]
  function = "locale-redirect"
  path = "/"
