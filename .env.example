# CASCADE: Copy to .env or .env.local and fill in values
# Public key is exposed to the client; private stays server-side only.

# Public VAPID key (from web-push generate-vapid-keys)
PUBLIC_VAPID_PUBLIC_KEY=

# Private VAPID key (server only)
VAPID_PRIVATE_KEY=

# Contact string, e.g. mailto:you@example.com
VAPID_CONTACT=mailto:you@example.com

# Optional: protect broadcast endpoint
PUSH_ADMIN_TOKEN=change_me_admin_token

# Google Analytics 4 measurement ID (public)
PUBLIC_GA_ID=G-57N05H2588
